'use strict';

angular.module('trng.trainer.training.main').controller('trainerTrainingController', [
    '$log',
    '$scope',
    '$state',
    'StatesNames',
    function($log, $scope, $state, StatesNames) {
        $scope.init = function() {
        };

        $scope.init();
    }
]);