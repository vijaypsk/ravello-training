'use strict';

angular.module('trng.trainer').controller('trainerController', [
    '$scope',
    '$state',
    function($scope, $state) {
        $scope.init = function() {
        };

        $scope.init();
    }
]);
