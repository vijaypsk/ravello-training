'use strict';

(function(angular) {
	angular.module('trng.labs.main', ['ui.router'])
		.config(['$urlRouterProvider', '$stateProvider', function($urlRouterProvider, $stateProvider) {
//			$urlRouterProvider.when('/labs', '/labs/sessions');
		}]);
})(angular);
