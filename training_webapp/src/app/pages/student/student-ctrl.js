'use strict';

angular.module('trng.student').controller('studentController', [
    '$scope',
    '$state',
    function($scope, $state) {
        $scope.init = function() {
        };

        $scope.init();
    }
]);
