'use strict';

angular.module('trng.admin').controller('adminController', [
    '$scope',
    '$state',
    function($scope, $state) {
        $scope.init = function() {
        };

        $scope.init();
    }
]);
