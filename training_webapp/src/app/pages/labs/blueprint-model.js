'use strict';

(function (angular) {
    angular.module('trng.labs.labs').factory('trng.labs.labs.BlueprintModel', [
        '$q', '$log',
        function ($q, $log) {

            var blueprintsLoaded = false;

            var blueprints = [];

            var service = {
            };

            return service;
        }]);
})(angular);